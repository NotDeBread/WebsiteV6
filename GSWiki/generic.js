const index = {
    gameplay: {
        items: {
            
        },
        bosses: {
            
        },
        achievements: {
            
        }
    },
    modding: {

    },
    history: {

    }
}

function renderList(obj, parent) {
    for(key in Object.keys(obj)) {
        const listItem = 'e'
    }
}